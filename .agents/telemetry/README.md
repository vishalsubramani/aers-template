# Trajectory Telemetry

Emit normalized JSONL events aligned where practical with OpenTelemetry GenAI concepts: agent/model spans,
tool calls, nested roles, token/cost attributes, policy decisions, and evaluation outcomes. Content capture is
off and redacted by default. Trace the path, not only the final answer. Store run evidence outside the branch.
