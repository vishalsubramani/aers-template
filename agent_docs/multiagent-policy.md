# Multi-Agent Policy

Parallelize read-only exploration, research, review, threat modeling, and alternative generation. Keep writes
single-threaded unless partitions have explicit non-overlapping paths, frozen interfaces, independent checks,
and a merge queue. Shared contracts, migrations, dependencies, and integration are serialized. More agents
are a cost and coordination risk, not a maturity badge.
