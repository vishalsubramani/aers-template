# Evaluations

`public-cases.jsonl` is a visible smoke/adversarial suite, not a capability benchmark. Production promotion
requires external private holdouts, contamination checks, multiple trials, evaluator health checks, and a
frozen baseline. Do not optimize prompts or skills against the only oracle used to approve them.
