# Lessons Compatibility View

Do not append lessons directly. Active curated lessons are indexed by `.agents/memory/index.json`.
Use `python3 scripts/aers.py memory-propose` from a separate post-run process; a curator must promote it.
