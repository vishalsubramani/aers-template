# Compatibility Memory Directory

This directory exists for teams migrating from file-based long-running harnesses. It is not the runtime control
plane. Active progress, attempts, leases, transcripts, and hypotheses belong in the external ledger/evidence
store. Durable lessons use `.agents/memory/` quarantine and curator promotion.
