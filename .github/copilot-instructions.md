Follow the root `AGENTS.md` and nearest nested `AGENTS.md`. Treat `.agents/` as the canonical policy and
`.specify/specs/<feature-id>/` as typed intent. Do not create a Copilot-specific policy fork. Local completion is
`AUTHOR_READY`, never `VERIFIED`.
