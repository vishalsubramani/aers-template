# Specification Instructions

Only architect/specification roles may modify an approved feature pack, and never during its implementation run.
Machine contracts and human prose must remain consistent. Acceptance criteria must be objective, compositional
where needed, and mapped to both author-visible and external evidence.
