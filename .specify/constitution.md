# Specification Constitution

The canonical engineering constitution is `.agents/constitution.md`. This bridge keeps the repository
compatible with GitHub Spec Kit-style workflows without duplicating policy. Feature specifications must
conform to that constitution and to `.agents/schemas/feature-contract.schema.json`.
